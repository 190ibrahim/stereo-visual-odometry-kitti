dataset:
  sequence: "00"

vo:
  detector: 'sift'          # Feature detector type
  max_frames: null           # Number of frames to process
  plot: true                # Enable/disable visualization

visualization:
  show_video_stream: false   # Show live video stream of left camera
  show_matches: true        # Show feature matches between frames
  show_depth_map: true      # Show depth maps during processing
  show_disparity_map: true  # Show disparity maps during processing
  update_interval: 100      # Show visualizations every N frames
